import React from 'react';

const Recipe = ({singleChef}) => {
    // const { img, id, name, experience, recipes, like } = singleChef;

    return (
        <div>
            {/* <img src={img} alt="" /> */}
        </div>
    );
};

export default Recipe;